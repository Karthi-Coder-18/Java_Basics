package practice;

public class RelationalOperator {

    public static void main(String[] args) {
        int x = 19;
        int y = 100;

        boolean h = x < y;
        boolean k = x > y;
        boolean n = x == y;
        boolean l = x != y;
        boolean i = x <= y;
        boolean a = x >= y;

        // We have less than, greater than, less than or equal to, greater than or equal to, equal to, not equal to
    }


}
